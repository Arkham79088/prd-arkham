# {需求名称} — 产品需求文档（PRD）

---

## 1. 基本信息

| 字段 | 内容 |
|------|------|
| 需求编号 | {市场-系统-模块-需求名} |
| 版本 | {v1.0} |
| 状态 | {📝 Draft / 📋 Review / ✅ Released / 📦 Archived} |
| 所属系统 | {系统名} |
| 所属模块 | {模块名（模块代码）} |
| 创建日期 | {YYYY-MM-DD} |

---

## 2. 需求概述

### 2.0 需求背景

{说明业务痛点或优化动机，以及这个需求服务的用户角色。无需写成用户故事格式，直接陈述背景即可。}

### 2.1 需求边界与关联

**本需求包含：**
- {功能点 1}
- {功能点 2}

**本需求不包含：**

| 不包含的区域 | 归属需求 | 说明 |
|------------|---------|------|
| {区域描述} | {需求编号} | {说明} |

**关联需求：**

| 关联需求编号 | 关联类型 | 说明 |
|------------|---------|------|
| {编号} | {页面共享 / 数据依赖 / 流程衔接} | {说明} |

**原型页面归属：**

| 页面 | 精度 | 说明 |
|------|------|------|
| P01 {页面名} | 🟢 权威 | 本需求完整定义此页面 |
| P02 {页面名} | 🟡 占位 | 以 {需求编号} 为准 |

> 🟢 权威 = 本需求负责该页面的完整定义；🟡 占位 = 该页面仅为导航/上下文展示，以关联需求为准。

---

## 3. 功能描述

<!--
  每个页面/弹窗为一个功能小节（3.1、3.2...）。
  每个功能小节内部按 UI 区域拆解，每个区域逐组件描述。
  不使用 R-001 纯流水编号，改为「区域 → 组件」结构化命名。
-->

### 3.1 {页面/弹窗名称}

**功能说明：**
{概括这个页面/弹窗的作用、入口、服务角色。}

---

#### 🔹 {区域名称A}（如：筛选区域 / 表单配置区 / 来源信息区）

{一句话说明这个区域的整体用途。}

| 字段 | 组件类型 | 规则说明 |
|------|---------|---------|
| {字段名，如"到期日期"} | {组件类型，如"日期范围选择器"} | {具体规则：筛选逻辑、默认值、格式要求、校验等} |
| {字段名} | {组件类型} | {规则说明} |

> 组件类型常见值：文本输入框、下拉单选、下拉多选、日期选择器、日期范围选择器、
> 单选按钮组、复选框组、开关、数字输入框、文件上传区、只读展示、主操作按钮、
> 次操作按钮、标签组、搜索框 等。按实际情况命名，不局限于此列表。

---

#### 🔹 {区域名称B}（如：列表区域 / 数据看板 / 操作栏）

{区域说明。}

| 字段 | 组件类型 | 规则说明 |
|------|---------|---------|
| {字段名} | {组件类型} | {规则说明} |

---

#### 🔹 跨区域规则

{描述不属于单一区域但影响整个页面的规则，如互斥逻辑、联动计算、实时刷新策略等。}

| 规则 | 说明 |
|------|------|
| {规则名，如"筛选互斥逻辑"} | {具体描述} |

---

**原型 — P01 {页面名}**

页面路径：{系统} > {模块} > {页面}

布局说明：
{用文字描述页面整体布局，如：顶部为筛选栏，中部为数据列表，右上角有「新建」主操作按钮，底部为分页控件。弹窗类页面描述各区域的上下排列顺序和关系即可。}

交互说明：

| 区域 > 组件 | 操作 | 交互行为 |
|------------|------|---------|
| {区域A} > {字段名} | {操作描述，如"选择日期范围"} | {交互结果} |
| {区域B} > {字段名} | {操作描述} | {交互结果} |
| {区域C} > {按钮名} | {操作描述} | {交互结果} |

---

### 3.{n} 全局交互规范

{描述本需求涉及页面通用的反馈方式，如：}

| 场景 | 方式 |
|------|------|
| 操作成功 | {Toast 绿色，X 秒消失} |
| 操作失败 | {Toast 红色 / 行内错误} |
| 危险操作 | {二次确认弹窗} |
| 空状态 | {图标 + 文案占位} |
| 互斥冲突 | {静默取消冲突项} |
| 提交按钮启用条件 | {哪些字段必填后启用} |

---

## 4. 异常处理

| 编号 | 场景描述 | 处理方式 | 用户提示 |
|------|---------|---------|---------|
| E-001 | {描述} | {处理逻辑} | {展示给用户的文案} |

---

## 5. 上下游依赖

| 依赖方 | 依赖内容 | 说明 |
|-------|---------|------|
| {系统/团队/第三方} | {依赖的能力或产出} | {影响说明} |

---

## 6. 原型文件

| 文件 | 说明 |
|------|------|
| `原型.html` | 交互原型，浏览器直接打开 |
| `原型截图/P0X-xxx.png` | 原始设计截图（如有） |

---

<!--
  ============================================================
  以下为增量版本模板（v2.0+ 使用，v1.0 不需要）
  ============================================================
-->

# {需求名称} — 增量变更文档（v{N}.0 基于 v{N-1}.0）

---

## 1. 变更概述

| 字段 | 内容 |
|------|------|
| 需求编号 | {编号} |
| 基线版本 | v{N-1}.0 |
| 当前版本 | v{N}.0 |
| 变更日期 | {YYYY-MM-DD} |

本版本变更范围：
- {一句话概括变更1}
- {一句话概括变更2}

---

## 2. 变更明细

### [新增] {功能区域 / 组件名}

{完整描述新增功能，格式与 v1.0 PRD 中的区域/组件描述一致。}

#### 🔹 {新增区域名称}

| 字段 | 组件类型 | 规则说明 |
|------|---------|---------|
| {字段名} | {组件类型} | {规则说明} |

---

### [修改] {功能区域 / 组件名}

**变更前（v{N-1}.0）：**
{简要引用原规则}

**变更后（v{N}.0）：**
{完整描述变更后的规则}

**变更原因：**
{为什么改}

---

### [删除] {功能区域 / 组件名}

**原规则（v{N-1}.0）：**
{引用被删除的规则}

**删除原因：**
{为什么删}

---

## 3. 关联影响

| 受影响需求 | 影响内容 | 是否需同步修改 |
|-----------|---------|--------------|
| {编号} | {说明} | {是/否} |

---

## 4. 异常处理（仅新增/变更部分）

| 编号 | 场景描述 | 处理方式 | 用户提示 |
|------|---------|---------|---------|
| E-{NNN} | {描述} | {处理逻辑} | {文案} |

---

## 5. 原型文件

| 文件 | 说明 |
|------|------|
| `原型.html` | 本版本完整最新原型 |

---

<!--
  ============================================================
  以下为 APP 需求专项章节（仅 APP 需求使用，按用户确认的项选填）
  ============================================================
-->

## A. 埋点整理（APP 专项）

> 仅 APP 需求，且用户确认需要时填写。

| 事件名 | 触发时机 | 携带参数 | 说明 |
|-------|---------|---------|------|
| {event_name} | {用户完成某操作时} | {param1: 值说明, param2: 值说明} | {补充说明} |

---

## B. 灰度发布节奏（APP 专项）

> 仅 APP 需求，且用户确认需要时填写。

| 阶段 | 范围 | 持续时间 | 放量条件 |
|------|------|---------|---------|
| 第一阶段 | {内测 / 1% 用户 / 指定设备} | {X 天} | {无异常则进入下一阶段} |
| 第二阶段 | {10% 用户} | {X 天} | {核心指标正常} |
| 全量 | 100% | — | {满足条件后全量} |

灰度中止条件：{如崩溃率上升 X%、核心转化下降 Y% 则回滚}

---

## C. 分流逻辑（APP 专项）

> 仅 APP 需求，且用户确认需要时填写。

| 分组 | 比例 | 触发条件 | 体验差异 |
|------|------|---------|---------|
| 实验组 | {X%} | {如：新注册用户 / 指定地区} | {新功能} |
| 对照组 | {Y%} | {其余用户} | {原有逻辑} |

互斥实验：{说明是否与其他实验互斥，如互斥则描述优先级}

---

## D. BI 数据提取逻辑（APP 专项）

> 仅 APP 需求，且用户确认需要时填写。每项对应一张报表或一个数据提取需求。

### D.1 {数据主题，如"新功能使用漏斗"}

**目的：** {说明这份数据要回答什么业务问题}

**数据来源：** {埋点事件名 / 日志表名}

**提取逻辑：**

| 指标 | 计算方式 | 筛选条件 |
|------|---------|---------|
| {指标名} | {COUNT / SUM / 漏斗计算} | {时间范围、用户分群等} |

**输出频率：** {每日 / 每周 / 按需}

**负责人：** {BI 负责人或提数方}
